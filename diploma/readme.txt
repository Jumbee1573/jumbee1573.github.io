всю страницу заполнить инфой +
скрипты глянуть +
баг с js (нельзя вынести переменные из функции) +
баг с menu css (нелья перенести фрагмент кода) +
показать еще (скрыть курсы) // 1 строку оставить, а остальные скрыть +
переделать на главной лого и поправить шрифт +
кнопка перемещения вверх (back to top) +
страница с авторизацией студентов +
страница с авторизацией преподов +
add patterns for inputs +
remove scroll when preloader works +
(студент может дать пароль другому, и во время занятия один решит 2 теста) ???(сессии js)
next and prev button on the pages ???( links)
save the current state in the course ??? (cookies)
when you selected point in menu on the small screen

авторизация регистрация( сессии)
здравствуй, студент петров
(админка 


<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>

$().addclass(document.cookie.name);

лагает кнопка запуска тестов(если из другого браузера, то оно плохо работает)(перебивает если из 2 браузеров на одном акк)